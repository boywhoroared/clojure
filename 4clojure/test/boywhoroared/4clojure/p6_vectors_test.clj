(ns
 ^{:difficulty "elementary", :tags [], :description "Vectors can be constructed several ways.\nYou can compare them with lists."} boywhoroared.4clojure.p6-vectors-test
 (:require [clojure.test :refer [deftest is testing]]))

(defn
 __
 [& args]
 ^{:line 33, :column 17}
 (comment "Write your solution inside this function"))

(deftest
  problem-6-test
  (testing
   "Problem 6: Vectors can be constructed several ways.\nYou can compare them with lists."
  ;(is (= [__] (list :a :b :c) (vec '(:a :b :c)) (vector :a :b :c)))
  (is (= [:a :b :c] (list :a :b :c) (vec '(:a :b :c)) (vector :a :b :c)))))
  ;; Fill in vector literal
 

