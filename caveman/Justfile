alias repl := nrepl

# Shows available recipes
help:
  just --list

run:
  clojure -M -m example.main

nrepl:
  clojure -M:dev:repl/nrepl

conjure:
  clojure -M:dev:repl/conjure

rebel:
  clojure -M:dev:repl/rebel

nrebel:
  clojure -M:dev:repl/nrebel

check *FLAGS:
  clojure -T:cljfmt check -- {{FLAGS}}

fix *FLAGS: 
  clojure -T:cljfmt fix -- {{FLAGS}}
